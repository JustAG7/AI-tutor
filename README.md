# Verdant Scholar Frontend

React Native frontend scaffolded from the `stitch_ai_english_learning_ui` HTML mockups.

## Run

```bash
npm install
npm run start
```

## Scope

- Frontend only
- No backend or database
- Uses local mock data
- Removes all simulated phone-frame wrappers from the original HTML references
